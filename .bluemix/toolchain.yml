version: '2'

template:
  name: "DYML: Simple"
  description: "DYML: Simple"
  required:
    - repo

  info:
    git url: >
      "[" + $env.repository + "]" +
        "(" + $env.repository + ")"
    git branch: >
      "[" + $env.branch + "]" +
        "(" + $env.repository +
          "/tree/" + $env.branch + ")"

toolchain:
  name: 'DYML: Simple'

services:
  repo:
    service_id: hostedgit
    parameters:
      test4: >
        $env.services.repo.service_id ? $env.services.repo.service_id : "don't have it!"

form:
  pipeline:
    parameters: {}
    schema: {}
